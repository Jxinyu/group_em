# group_em

This repository contains the full group_em project.

- `backend/`: Java Spring Boot backend
- `frontend/`: Vue frontend

Runtime secrets are read from environment variables. Configure database, mail,
JWT, and Aliyun credentials locally before running the backend.
