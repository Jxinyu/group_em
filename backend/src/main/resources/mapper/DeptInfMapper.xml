<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gp.mapper.DeptInfMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.gp.pojo.DeptInf">
        <id column="ID" property="id" />
        <result column="NAME" property="name" />
        <result column="REMARK" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ID, NAME, REMARK
    </sql>

    <select id="queryOneDept" parameterType="int" resultType="DeptInf">
        select *
        from dept_inf
        <where>
            <if test="id != null">
                and ID like concat('%', #{id}, '%')
            </if>
            <if test="name != null">
                and NAME like concat('%', #{name}, '%')
            </if>
            <if test="remark != null">
                and REMARK like concat('%', #{remark}, '%')
            </if>
        </where>
    </select>

    <select id="queryAllDept" resultType="DeptInf">
        select * from dept_inf;
    </select>

    <insert id="addDept" parameterType="DeptInf">
        insert into dept_inf (ID, NAME, REMARK)
        values
        (#{id},#{name}, #{remark});
    </insert>

    <update id="updateDept" parameterType="DeptInf">
        update dept_inf
        set NAME=#{name}, REMARK=#{remark}
        where ID=#{id};
    </update>
    <delete id="deleteDept" parameterType="int">
        delete
        from dept_inf
        where ID=#{id};
    </delete>

</mapper>
